import Button from "@/components/ui/Button";

export default async function Home() {
  // const res = await db.get("Animal");

  return (
    <main className="flex min-h-screen flex-col items-center justify-between p-24">
      <Button>Submit</Button>
    </main>
  );
}
